apiVersion: v1
kind: Service
metadata:
  name: ejemplo-php
  labels:
    app: ejemplo-php
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ejemplo-php
