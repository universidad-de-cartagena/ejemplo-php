apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ejemplo-php.fullname" . }}
  labels:
    {{- include "ejemplo-php.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
